import { Action } from "@reduxjs/toolkit";

const initialState = {};

function reducer(state: any, action: Action) {
  switch (action.type) {
    default:
      return state;
  }
}

export default reducer;
